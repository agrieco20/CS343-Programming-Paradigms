=begin #comment
Name: Anthony Grieco
Date: 9/28/2022

Description: This program converts numbers 0-255 and converts them into Binary Array Format. For example, "5" is returned as [0,0,0,0,0,1,0,1]
=end #comment

def binaryArrayFormatter(currentNum)
  #Place ONLY the implementation to convert the given number (as a parameter) into Binary within this function (returns Array of the Binary Format for only the given number)
  #Conversion would be easiest by finding the remainder of a value (% the given number by 2 every time)

  # arrayLengthDeterminer = currentNum
  # arrayLengthCounter = 0
  #
  # while arrayLengthDeterminer > 0
  #   arrayLengthDeterminer /= 2
  #   arrayLengthCounter += 1
  # end

  binaryArray = Array.new(8) #Temporary Parameter Values (Probably need to make it so that it could accept any input and return a binary value)
  counter = binaryArray.length - 1

  binaryArray.size

  # arrayTest[arrayTest.length - 1] = 1 #Temporary
  # puts arrayTest[0...arrayTest.length] #Temporary


  # test1 = currentNum % 2
  # test2 = currentNum /= 2
  # test1

  #Calculates what the Binary Value of a number should be, assigns the value in reverse order to "binaryArray" and then "returns" it in the following line back to the main program
  while counter >= 0 #do {
    binaryArray[counter] = currentNum % 2
    currentNum /= 2
    counter -= 1
    #}
  end
  binaryArray #Because this is the last line of the function, this is the item that the function "returns" when it is called
end

if __FILE__ == $0
#Place code below in main function
  #----
  digits = Array(0...256)
  # puts "digits

  digits.each_index do|index| # { |x| print x, "--"}# do
    # print digits[index]  #Needs to print out the original number followed by its binary array representation (preferably in the same line)
    print digits[index] , ": " , binaryArrayFormatter(digits[index]) , "\n"#(digits[index]) #Calls the "binaryArrayFormatter" function and converts the current index of the array into a Binary Array
  end
  #----
end
